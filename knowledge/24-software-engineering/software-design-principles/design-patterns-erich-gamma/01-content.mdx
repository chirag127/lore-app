---
title: Core Concepts
subtitle: The 23 Gang of Four Patterns — Creational, Structural, and Behavioral
---

## Pattern Classification

```mermaid
flowchart TD
    subgraph GoF["GoF Design Patterns"]
        CRE["Creational<br/>(object creation)"]
        STR["Structural<br/>(object composition)"]
        BEH["Behavioral<br/>(object interaction)"]
    end

    subgraph Creation["5 Patterns"]
        C1["Singleton"]
        C2["Factory Method"]
        C3["Abstract Factory"]
        C4["Builder"]
        C5["Prototype"]
    end

    subgraph Structure["7 Patterns"]
        S1["Adapter"]
        S2["Decorator"]
        S3["Facade"]
        S4["Proxy"]
        S5["Composite"]
        S6["Bridge"]
        S7["Flyweight"]
    end

    subgraph Behavior["11 Patterns"]
        B1["Observer"]
        B2["Strategy"]
        B3["Command"]
        B4["Template Method"]
        B5["Iterator"]
        B6["State"]
        B7["Visitor"]
        B8["Mediator"]
        B9["Chain of Responsibility"]
        B10["Memento"]
        B11["Interpreter"]
    end

    CRE --> Creation
    STR --> Structure
    BEH --> Behavior
```

---

## Singleton Pattern

```mermaid
flowchart LR
    subgraph Singleton["Singleton Pattern"]
        CLIENT["Client code"]
        S1["Singleton<br/>instance = null"]
        S2["getInstance()"]
        S3["if instance == null<br/>instance = new Singleton()"]
        S4["return instance"]
    end

    CLIENT --> S2
    S2 --> S3
    S3 --> S4
    S4 --> CLIENT
```

Ensures a class has only one instance and provides a global point of
access. Controversial because it introduces global state, making
testing difficult.

---

## Observer Pattern

```mermaid
flowchart LR
    subgraph Observer["Observer Pattern"]
        SUBJECT["Subject<br/>(observable)"]
        O1["Observer A<br/>(update)"]
        O2["Observer B<br/>(update)"]
        O3["Observer C<br/>(update)"]
        STATE["State changes"]
    end

    STATE --> SUBJECT
    SUBJECT -->|"notify()"| O1
    SUBJECT -->|"notify()"| O2
    SUBJECT -->|"notify()"| O3
```

Defines a one-to-many dependency so that when one object changes state,
all its dependents are notified. Fundamental to event-driven
programming, UI frameworks, and reactive systems.

---

## Strategy Pattern

```mermaid
flowchart LR
    subgraph Strategy["Strategy Pattern"]
        CONTEXT["Context"]
        S1["Strategy A<br/>(e.g., CreditCard)"]
        S2["Strategy B<br/>(e.g., PayPal)"]
        S3["Strategy C<br/>(e.g., Crypto)"]
        ALGO["algorithm()"]
    end

    CONTEXT --> ALGO
    ALGO --> S1
    ALGO --> S2
    ALGO --> S3
```

Defines a family of algorithms, encapsulates each one, and makes them
interchangeable. Strategy lets the algorithm vary independently from
the clients that use it.

---

## Decorator Pattern

```mermaid
flowchart LR
    subgraph Decorator["Decorator Pattern"]
        COMP["Component<br/>(interface)"]
        CONC["ConcreteComponent"]
        D1["Decorator A"]
        D2["Decorator B"]
    end

    COMP --> CONC
    COMP --> D1
    COMP --> D2
    D1 -->|"wraps"| CONC
    D2 -->|"wraps"| D1
```

Attaches additional responsibilities to an object dynamically.
Decorators provide a flexible alternative to subclassing for extending
functionality (Open/Closed Principle).

---

## Key Lessons

- **Patterns are not goals; they are tools.** The goal is clean,
  maintainable code. If a pattern makes things worse, do not use it.
- **Recognize when patterns are overused.** Singleton abuse,
  unnecessary factories, and Visitor for everything are common
  antipatterns.
- **Design for change.** Most patterns exist to isolate the impact of
  future changes. Identify what varies and encapsulate it.
- **The pattern form matters.** The GoF pattern template (Intent,
  Motivation, Applicability, Structure, Consequences) became the
  standard for documenting design knowledge.

---

## Practical Applications

- **Singleton:** Logging, configuration managers, thread pools (but
  prefer dependency injection in modern code)
- **Observer:** Event listeners, pub/sub systems, MVC architecture
- **Strategy:** Payment processing, sorting algorithms, validation
  rules
- **Decorator:** Java I/O streams, middleware pipelines, caching
  layers
- **Factory Method:** Object creation with unknown concrete types,
  framework hooks
- **Adapter:** Wrapping legacy code, making incompatible interfaces
  compatible
- **Facade:** Simplifying complex subsystem APIs (e.g., a
  `VideoConverter` class)
- **Command:** Undo/redo, job queues, transaction logging
